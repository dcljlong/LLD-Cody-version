# LLD Cody frontend example environment
# Copy to .env locally, or set in Vercel Project Settings > Environment Variables.
# For Create React App, frontend-exposed variables must start with REACT_APP_.

REACT_APP_BACKEND_URL=http://127.0.0.1:8010
WDS_SOCKET_PORT=3000
ENABLE_HEALTH_CHECK=false
